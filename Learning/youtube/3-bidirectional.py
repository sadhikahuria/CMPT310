"""
Bidirectional search
- search from start to goal and goal to start
- when two searches meet, the path is found
time complexity: O(2b^(d/2)), only average case
- complte if bfs
- incomplete if dfs




"""
